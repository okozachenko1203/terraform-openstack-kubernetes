module"create-k8s-infra"{
    source="./create-k8s-infra"
    cloud_user_name="${var.cloud_user_name}"
    cloud_user_pass="${var.cloud_user_pass}"
    cloud_auth_url="${var.cloud_auth_url}"
    cloud_tenant_id="${var.cloud_tenant_id}"
    external_network_name="${var.external_network_name}"
    master_flavor_id="${var.master_flavor_id}"
    cloud_network_id="${var.cloud_network_id}"
    cluster_VIP="${var.cluster_VIP}"
    cluster_name="${var.cluster_name}"
    vms_image_id="${var.vms_image_id}"
    vms_ssh_key="${var.vms_ssh_key}"
    master_count="${var.master_count}"
    workers_count="${var.workers_count}"
    worker_flavor_id="${var.worker_flavor_id}"
}